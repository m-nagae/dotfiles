#!/bin/bash

# Install fisherman
[ ! -e ~/.config/fish/functions/fisher.fish ] && curl -Lo ~/.config/fish/functions/fisher.fish --create-dirs git.io/fisherman

# Install anyenv
[ ! -d ~/.anyenv ] && git clone https://github.com/riywo/anyenv ~/.anyenv
